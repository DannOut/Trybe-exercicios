// import { REGISTER_COSTUMER } from '../actions/types';

const INITIAL_STATE = [];

export const registerReducer = (state = INITIAL_STATE, { type, payload }) => {
  switch (type) {

  // TODO falta fazer o CASE REGISTER_COSTUMER

    default:
      return state;
  }
};
